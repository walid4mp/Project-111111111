import { NextResponse } from 'next/server';
import { supabaseFetch } from '@/app/lib/server/supabase';
export async function POST(req:Request){try{const {email,token}=await req.json();if(!email||!token)return NextResponse.json({error:'Email and code are required.'},{status:400});const r=await supabaseFetch('/auth/v1/verify',{method:'POST',body:JSON.stringify({email:String(email).trim().toLowerCase(),token:String(token).trim(),type:'email'})});const data=await r.json().catch(()=>({}));if(!r.ok)return NextResponse.json({error:data.msg||'Invalid or expired code.'},{status:400});const out=NextResponse.json({ok:true,user:data.user});if(data.access_token)out.cookies.set('warhex_access_token',data.access_token,{httpOnly:true,sameSite:'lax',secure:process.env.NODE_ENV==='production',path:'/',maxAge:3600});if(data.refresh_token)out.cookies.set('warhex_refresh_token',data.refresh_token,{httpOnly:true,sameSite:'lax',secure:process.env.NODE_ENV==='production',path:'/',maxAge:2592000});return out;}catch{return NextResponse.json({error:'Unable to verify code.'},{status:500});}}
