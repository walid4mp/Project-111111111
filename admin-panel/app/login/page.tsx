'use client';
import {FormEvent,useState} from 'react';import{useRouter}from'next/navigation';
const API=process.env.NEXT_PUBLIC_API_URL||'https://project-111111111.onrender.com';
export default function Login(){const router=useRouter();const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[error,setError]=useState('');const[loading,setLoading]=useState(false);
async function submit(e:FormEvent){e.preventDefault();setError('');setLoading(true);try{const r=await fetch(`${API}/api/admin/login`,{method:'POST',headers:{'Content-Type':'application/json','X-WarHex-Client':'admin'},body:JSON.stringify({email,password})});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||'تعذر تسجيل دخول المدير');if(!d.access_token)throw new Error('الخادم لم يعُد رمز جلسة المدير.');localStorage.setItem('warhex_admin_access_token',d.access_token);localStorage.setItem('warhex_admin_refresh_token',d.refresh_token||'');router.replace('/dashboard')}catch(e){setError(e instanceof Error?e.message:'حدث خطأ');setLoading(false)}}
return <main className="shell" style={{minHeight:'100vh',display:'grid',placeItems:'center',padding:20}}><section className="panel" style={{width:'min(460px,100%)',borderRadius:30,padding:30}}><div style={{textAlign:'center',marginBottom:28}}><img src="/icon-512.png" style={{width:82,height:82,borderRadius:24,objectFit:'cover'}}/><h1 className="gradient" style={{fontSize:34,margin:'15px 0 5px'}}>WarHex Admin</h1><p className="muted">لوحة الإدارة مستقلة عن تطبيق المستخدم</p></div><form onSubmit={submit} style={{display:'grid',gap:14}}><label>البريد الإلكتروني<input className="input" type="email" value={email} onChange={e=>setEmail(e.target.value)} required placeholder="admin@example.com"/></label><label>كلمة المرور<input className="input" type="password" value={password} onChange={e=>setPassword(e.target.value)} required placeholder="••••••••"/></label>{error&&<div style={{padding:12,borderRadius:12,background:'#ef44441a',color:'#fca5a5'}}>{error}</div>}<button className="btn" disabled={loading}>{loading?'جارٍ التحقق…':'دخول لوحة الإدارة'}</button></form></section></main>}
