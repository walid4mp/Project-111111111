# WarHex Android build

The Android platform folder is generated by Flutter in CI because the repository intentionally keeps the Flutter app source lightweight.

In GitHub Actions, `flutter create --platforms=android --project-name warhex_app .` runs before `flutter pub get` and `flutter build apk`.

Artifact: `WarHex-APK/WarHex.apk`.
