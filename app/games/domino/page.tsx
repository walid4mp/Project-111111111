'use client';
import {useState} from 'react'; import Link from 'next/link'; import Button from '@/app/components/ui/Button'; import Card from '@/app/components/ui/Card';
const tiles=['6|6','6|2','2|5','5|1','1|4','4|3','3|0'];
export default function Domino(){const [hand,setHand]=useState(tiles.slice(0,6));const [chain,setChain]=useState<string[]>(['6|3']);const play=(t:string)=>{setHand(h=>h.filter(x=>x!==t));setChain(c=>[...c,t]);setTimeout(()=>{setChain(c=>[...c,'🤖']);},500)};return <main className="min-h-screen pb-24 px-3 py-4 max-w-3xl mx-auto"><div className="flex justify-between mb-4"><Link href="/games"><Button variant="ghost">← Games</Button></Link><b>🁫 DOMINO • LIVE</b></div><Card variant="premium" className="p-4"><div className="flex justify-between text-sm mb-5"><span>Room #64578</span><span className="text-emerald-400">● 4 players</span></div><div className="min-h-44 rounded-2xl bg-[#4b2b18] p-4 flex flex-wrap gap-2 items-center justify-center border border-yellow-700/40">{chain.map((x,i)=><div key={i} className="bg-white text-black rounded-lg px-3 py-2 font-bold shadow">{x==='🤖'?'🤖':x.split('|').join(' • ')}</div>)}</div><p className="text-center text-sm text-gray-400 mt-4">Your turn — select a tile</p><div className="flex gap-2 overflow-x-auto py-4">{hand.map(t=><button key={t} onClick={()=>play(t)} className="min-w-16 bg-white text-black rounded-xl p-3 font-bold">{t.split('|')[0]}<hr/>{t.split('|')[1]}</button>)}</div><div className="grid grid-cols-4 text-center text-xs text-gray-300"><span>Walid 👑</span><span>Ali ●</span><span>Sara ●</span><span>Yassine ●</span></div></Card></main>}
