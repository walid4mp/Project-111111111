import { NextResponse } from 'next/server';
import { requireUser, supabaseServiceFetch } from '@/app/lib/server/supabase';
export async function GET(request:Request){const auth=await requireUser();if(!auth)return NextResponse.json({error:'Unauthorized'},{status:401});const u=new URL(request.url);const roomId=u.searchParams.get('roomId');const since=u.searchParams.get('since')||'1970-01-01T00:00:00.000Z';if(!roomId)return NextResponse.json({signals:[]});const q=`/rest/v1/voice_signals?room_id=eq.${encodeURIComponent(roomId)}&created_at=gt.${encodeURIComponent(since)}&or=(recipient_id.is.null,recipient_id.eq.${encodeURIComponent(auth.user.id)})&select=*&order=created_at.asc&limit=200`;const r=await supabaseServiceFetch(q);return NextResponse.json({signals:r.ok?await r.json():[]});}
export async function POST(request:Request){const auth=await requireUser();if(!auth)return NextResponse.json({error:'Unauthorized'},{status:401});const b=await request.json().catch(()=>({}));const roomId=String(b.roomId||'');const type=String(b.type||'');if(!roomId||!['join','offer','answer','candidate','leave'].includes(type))return NextResponse.json({error:'Invalid signaling message.'},{status:400});const r=await supabaseServiceFetch('/rest/v1/voice_signals',{method:'POST',headers:{Prefer:'return=minimal'},body:JSON.stringify({room_id:roomId,sender_id:auth.user.id,recipient_id:typeof b.recipientId==='string'?b.recipientId:null,type,payload:b.payload??null})});if(!r.ok)return NextResponse.json({error:'Unable to signal.'},{status:500});return NextResponse.json({ok:true});}
